// Pretty much self explanatory,
// adds classes and removes classes
$("p").addClass("big-title ugly-color");
$("p").removeClass("ugly-color");


// The equivalent of
// innerHTML & innerText
$("p").text("sexy wexy");
$("p").html("<em>bruh momento</em>");

// Attributes can be changed with
// .attr
$("a").attr("href", "http://google.com");
